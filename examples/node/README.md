# Node Server Wallet Example

This example shows the minimum server-side setup for using `@lumen/core` with a Stellar testnet keypair.

```bash
LUMEN_SERVER_SECRET=S... pnpm --filter @lumen/example-node start
```
